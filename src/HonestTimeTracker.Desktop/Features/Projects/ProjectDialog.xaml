<Window x:Class="HonestTimeTracker.Desktop.Features.Projects.ProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Projekt"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="TitleText"
                   Text="New project"
                   Style="{StaticResource HeaderText}"
                   FontSize="18"
                   Margin="0,0,0,20" />

        <TextBlock Grid.Row="1" Text="Project name" Margin="0,0,0,6" />

        <TextBox Grid.Row="2"
                 x:Name="NameBox"
                 Padding="8,6"
                 FontSize="14"
                 BorderBrush="#CCCCCC"
                 BorderThickness="1"
                 Margin="0,0,0,20"
                 KeyDown="NameBox_KeyDown" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="90"
                    Background="#E0E0E0"
                    Foreground="Black"
                    Margin="0,0,8,0"
                    Click="Cancel_Click" />
            <Button x:Name="OkButton"
                    Content="Save"
                    Width="90"
                    Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
