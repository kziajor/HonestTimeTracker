<Window x:Class="HonestTimeTracker.Desktop.Features.Tasks.TaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zadanie"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="TitleText"
                   Text="New task"
                   Style="{StaticResource HeaderText}"
                   FontSize="18"
                   Margin="0,0,0,20" />

        <TextBlock Grid.Row="1" Text="Task title" Margin="0,0,0,6" />

        <TextBox Grid.Row="2"
                 x:Name="TitleBox"
                 Padding="8,6"
                 FontSize="14"
                 BorderBrush="#CCCCCC"
                 BorderThickness="1"
                 Margin="0,0,0,16"
                 KeyDown="TitleBox_KeyDown" />

        <TextBlock Grid.Row="3" Text="Planned time (hours)" Margin="0,0,0,6" />

        <TextBox Grid.Row="4"
                 x:Name="PlannedHoursBox"
                 Padding="8,6"
                 FontSize="14"
                 BorderBrush="#CCCCCC"
                 BorderThickness="1"
                 Margin="0,0,0,16"
                 Text="0" />

        <TextBlock Grid.Row="5" Text="Project" Margin="0,0,0,6" />

        <ComboBox Grid.Row="6"
                  x:Name="ProjectComboBox"
                  Padding="8,6"
                  FontSize="14"
                  BorderBrush="#CCCCCC"
                  BorderThickness="1"
                  Margin="0,0,0,20"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id" />

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="90"
                    Background="#E0E0E0"
                    Foreground="Black"
                    Margin="0,0,8,0"
                    Click="Cancel_Click" />
            <Button x:Name="OkButton"
                    Content="Save"
                    Width="90"
                    Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
