<Window x:Class="HonestTimeTracker.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Honest Time Tracker"
        Height="680" Width="1200"
        MinHeight="480" MinWidth="800"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="1" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}">
            <DockPanel>
                <!-- Logo area -->
                <Border DockPanel.Dock="Top"
                        Background="{StaticResource PrimaryBrush}"
                        Padding="20,16">
                    <TextBlock Text="Honest Time Tracker"
                               Style="{StaticResource HeaderText}"
                               FontSize="14"
                               FontWeight="Bold"
                               TextWrapping="Wrap" />
                </Border>

                <!-- Navigation -->
                <StackPanel DockPanel.Dock="Top" Margin="0,8,0,0">
                    <Button Style="{StaticResource NavButton}" Content="Today"    Tag="Today"    Click="NavButton_Click" />
                    <Button Style="{StaticResource NavButton}" Content="Projects" Tag="Projects" Click="NavButton_Click" />
                    <Button Style="{StaticResource NavButton}" Content="Tasks"    Tag="Tasks"    Click="NavButton_Click" />
                    <Button Style="{StaticResource NavButton}" Content="Records"  Tag="Records"  Click="NavButton_Click" />
                    <Button Style="{StaticResource NavButton}" Content="Reports"  Tag="Reports"  Click="NavButton_Click" />
                    <Button Style="{StaticResource NavButton}" Content="Settings" Tag="Settings" Click="NavButton_Click" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Divider -->
        <Rectangle Grid.Column="1" Fill="{StaticResource PrimaryBrush}" />

        <!-- Main content area -->
        <Border Grid.Column="2" Padding="32,28">
            <ContentControl x:Name="MainContent" />
        </Border>
    </Grid>
</Window>
